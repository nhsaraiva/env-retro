describe('Testing ExitRoomService', () => {
    it('should delete an player when exit with room', () => {
        
    });

    it('should return an exception when room has finished', () => {
        
    });

    it('should return an exception when player passed not exists', () => {
        
    });
});